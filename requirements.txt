# Core dependencies
numpy>=1.25.0
opencv-python>=4.12.0

# Hailo Runtime (system package, but listed for reference)
# Install via: sudo apt-get install hailo-runtime
# Or from Hailo SDK installation

# Optional: Approximate Nearest Neighbor for fast similarity search
annoy>=1.17.0

# Note: hailo_apps is installed via Hailo SDK, not via pip
# Make sure Hailo SDK is properly installed on your system

